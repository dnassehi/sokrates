// eksempel: tests/api/saveSummary.test.ts
describe('saveSummary API', () => {
  it('should 405 on GET', async () => {/* ... */});
  it('returns 401 without auth', async () => {/* ... */});
  it('returns 404 for invalid session', async () => {/* ... */});
  it('returns 200 and updates summary on valid request', async () => {/* ... */});
});